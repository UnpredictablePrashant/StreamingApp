apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "streamingapp.fullname" . }}-test-connection"
  labels:
    {{- include "streamingapp.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "streamingapp.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
